@using EGH01.Core;
@using EGH01.Models.EGH;
@using EGH01DB.Types;
@using EGH01DB.Primitives;

@model EGH01DB.ORTContext

@{

    Layout = "~/Views/Shared/EGHLayout.cshtml";
    Menu head = new Menu(
                          new Menu.MenuItem("Создать", "WaterCleaningMethod.Create", true),
                          new Menu.MenuItem("Excel-формат", "WaterCleaningMethod.Excel", true)
                        );

    List<WaterCleaningMethod> list = new List<WaterCleaningMethod>();
    if (@Model != null) { Helper.GetListWaterCleaningMethods(@Model, ref list); };

}


@Html.Partial("_TypeHeadMenu", new TypeViewMenu { Controller = "EGHORT", Action = "WaterCleaningMethod", Menu = head })
@Html.Partial("_WaterCleaningMethodList", list)