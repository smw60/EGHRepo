@using EGH01.Core;
@using EGH01.Models.EGH;
@using EGH01DB;
@using EGH01DB.Primitives;
@using EGH01DB.Types;
@model EGH01DB.ORTContext

@{

    Layout = "~/Views/Shared/EGHLayout.cshtml";
    Menu start = new Menu(
                          new Menu.MenuItem("Создать", "SoilPollutionCategories.Create", true),
                          new Menu.MenuItem("Отказаться", "SoilPollutionCategories.Cancel", true)
                        );

    Menu create = new Menu(
                          new Menu.MenuItem("Создать", "SoilPollutionCategories.Create.Create", true),
                          new Menu.MenuItem("Отказаться", "SoilPollutionCategories.Create.Cancel", true)
                        );

}

@Html.Partial("_TypeHeadMenu", new TypeViewMenu { Controller = "EGHORT", Action = "SoilPollutionCategories", Menu = start })
@using (Html.BeginForm("SoilPollutionCategoriesCreate", "EGHORT"))
{
    <div class="box">
        <fieldset form="SoilPollutionCategories">
            <div class="boxdata">
                <p>
                    <label class="boxdata-label-30">Наименование категории загрязнения грунта</label>
                    <textarea class="boxdata-input-50" name="Name"></textarea>
                </p>
                <p>
                    <label class="boxdata-label-40">Диапазон кратности превышения показателя ПДК <sup><small></small></sup></label>
                    <input class="boxdata-input-20" type="number" step="0.1" min="0" name="min" value=" " />
                    <input class="boxdata-input-20" type="number" step="0.1" min="0" name="max" value=" " />
                </p>
            </div>
        </fieldset>
        @Html.Partial("_TypeLineMenu", create)
    </div>
}

