@using EGH01.Core;
@using EGH01.Models.EGH;
@model EGH01DB.Types.PenetrationDepth
@{
    Layout = "~/Views/Shared/EGHLayout.cshtml";
    Menu head = new Menu(
                          new Menu.MenuItem("Создать", "PenetrationDepth.Create", false),
                          new Menu.MenuItem("Excel-формат", "PenetrationDepth.Excel", false)
                        );

    Menu delete = new Menu(
                          new Menu.MenuItem("Удалить", "PenetrationDepth.Delete.Delete", true),
                          new Menu.MenuItem("Отказаться", "PenetrationDepth.Delete.Cancel", true)
                        );

}
@Html.Partial("_TypeHeadMenu", new TypeViewMenu { Controller = "EGHORT", Action = "PenetrationDepth", Menu = head })

@using (Html.BeginForm("PenetrationDepthDelete", "EGHORT"))
{

    <div class="box">
        <div class="boxdata">
            <div class="boxdata">
                <label class="boxdata-label-30">Код типа категории</label>@Model.type_code.ToString()
                <input type="hidden" value="@Model.type_code.ToString()" name="code">
            </div>
            <div class="boxdata">
                <label class="boxdata-label-30">Наименование категории</label>@Model.name
            </div>
        </div>

        @Html.Partial("_TypeLineMenu", delete)

    </div>
}