@using EGH01.Core;
@using EGH01.Models.EGH;
@using EGH01DB.Types;
@using EGH01DB.Primitives;

@model EGH01DB.ORTContext

@{

    Layout = "~/Views/Shared/EGHLayout.cshtml";
    Menu head = new Menu(
                          new Menu.MenuItem("Создать", "WaterPollutionCategories.Create", true),
                          new Menu.MenuItem("Excel-формат", "WaterPollutionCategories.Excel", true)
                        );

    List<WaterPollutionCategories> list = new List<WaterPollutionCategories>();
    if (@Model != null) { Helper.GetListWaterPollutionCategories(@Model, ref list); };

}


@Html.Partial("_TypeHeadMenu", new TypeViewMenu { Controller = "EGHORT", Action = "WaterPollutionCategories", Menu = head })
@Html.Partial("_WaterPollutionCategoriesList", list)