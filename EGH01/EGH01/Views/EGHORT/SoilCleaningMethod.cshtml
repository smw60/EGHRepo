@using EGH01.Core;
@using EGH01.Models.EGH;
@using EGH01DB.Types;
@using EGH01DB.Primitives;

@model EGH01DB.ORTContext

@{

    Layout = "~/Views/Shared/EGHLayout.cshtml";
    Menu head = new Menu(
                          new Menu.MenuItem("Создать", "SoilCleaningMethod.Create", true),
                          new Menu.MenuItem("Excel-формат", "SoilCleaningMethod.Excel", true)
                        );

    List<SoilCleaningMethod> list = new List<SoilCleaningMethod>();
    if (@Model != null) { Helper.GetListSoilCleaningMethods(@Model, ref list); };

}


@Html.Partial("_TypeHeadMenu", new TypeViewMenu { Controller = "EGHORT", Action = "SoilCleaningMethod", Menu = head })
@Html.Partial("_SoilCleaningMethodList", list)