@using EGH01.Core;
@using EGH01.Models.EGH;
@using EGH01DB.Primitives;
@model EGH01DB.RGEContext

@{

    Layout = "~/Views/Shared/EGHLayout.cshtml";
    Menu head = new Menu(
                          new Menu.MenuItem("Создать", "WaterProperties.Create", true),
                          new Menu.MenuItem("Excel-формат", "WaterProperties.Excel", true)
                        );

    List<WaterProperties> list = new List<WaterProperties>();
    if (@Model != null) { Helper.GetListWaterProperties(@Model, ref list); };

}


@Html.Partial("_TypeHeadMenu", new TypeViewMenu { Controller = "EGHRGE", Action = "WaterProperties", Menu = head })
@Html.Partial("_WaterPropertiesList", list)


